/**
 * Set values for color themes.
 */
export enum ColorValues {
  HueMax = 360,
  HueMin = 0,
  SaturationMax = 90,
  SaturationMin = 30,
  LightnessMax = 65,
  LightnessMin = 35,
  MaxLightness = 80,
  MinLightness = 20
}
